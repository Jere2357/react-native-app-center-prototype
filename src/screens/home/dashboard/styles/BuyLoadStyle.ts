import {StyleSheet} from 'react-native';

const BuyLoadStyle = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default BuyLoadStyle;
