import {StyleSheet} from 'react-native';

const TransferFundsStyle = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default TransferFundsStyle;
