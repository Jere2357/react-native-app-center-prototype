import {StyleSheet} from 'react-native';

const ConstainerStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 24,
    paddingTop: 28,
    backgroundColor: '#fff',
  },
});

export default ConstainerStyles;
